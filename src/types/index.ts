/* Structure of JSON file with bot token */
declare interface AuthJson {
    TOKEN: string;
}

/* Structure of JSON file with bot config */
declare interface ConfigJson {
    CLIENT_ID: string;
    GUILD_ID: string;
}
